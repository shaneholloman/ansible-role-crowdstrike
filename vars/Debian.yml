---
# The name of the S3 object corresponding to the CrowdStrike Falcon
# sensor system package
package_object_name: falcon-sensor_7.05.0-16004_{{ 'amd64' if ansible_architecture == 'x86_64' else 'arm64' }}.deb
